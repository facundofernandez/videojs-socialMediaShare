var math = {

  square: function(n) {
    return n * n;
  },

  sum: function() {
    return Array.prototype.slice.call(arguments).reduce(function(t, n) {
      return t + n;
    }, 0);
  }
};

math.square(4);
math.add(1, 2, 3, 4, 5, 6);
